# v2.14.6-alpha.1 Evidence - HS1 KV Warm Cluster

## Result

`v2.14.6-alpha.1` passed the functional HS1/PR100 runtime gate from the real
ResilientDB fork.

```txt
runId=20260630T042133Z-hs1-kv
status=RUNTIME_SMOKE_PASSED
commit=2e1249d428c340581d887ef9c684ac83bb15e344
replicaCount=4
clientProcessCount=1
totalProcessCount=5
operationCount=30
passedOperations=30
clientTimeoutSec=30
```

## Command

```bash
docker run --rm \
  -e HS1_CLIENT_TIMEOUT_SEC=30 \
  -e HS1_OPERATION_COUNT=30 \
  -v "<fork>:/workspace" \
  -v chatay-bazel-cache:/root/.cache/bazel \
  -w /workspace \
  chatay-resilientdb-toolchain:bazel6-20260528 \
  bash tools/chatay/hs1/run_hs1_kv_warm_cluster.sh
```

## Evidence Checked

- Bazel build completed successfully for the HS1 KV service and client tools.
- Key and certificate generation completed for five local processes.
- Runtime started four consensus replicas plus one client/gateway process.
- Readiness trace reached `ready=5/5`.
- The runner created 30 `SET` client logs and 30 `GET` client logs.
- `client-get-030.log` returned the expected value:

```txt
chatay-hs1-value-20260630T042133Z-hs1-kv-030
```

## Raw Evidence Location

Raw logs were generated locally under:

```txt
documents/chatay-porting/v2.14.6-alpha.1/logs/20260630T042133Z-hs1-kv
```

The raw `logs/` tree is not committed because it contains ephemeral key and
certificate material generated by ResilientDB tooling. The reproducible command,
manifest fields, and validation result are committed here instead.

## Claim Boundary

This result proves that the HS1/PR100 KV path can run from fork-built
ResilientDB binaries and complete a 30-operation warm-cluster sequence. It does
not yet prove:

- benchmark superiority over PBFT;
- canonical HotStuff conformance;
- Byzantine behavior under faults;
- cold-start optimization.
