# v2.14.8-alpha.1 Evidence - HS1 NEWVIEW Bootstrap Parameters

## Status

Pending validation.

## Expected Evidence

This version needs at least:

- one default-compatible smoke run;
- one reduced-wait smoke run;
- `send_newview_policy` traces in node logs;
- explicit classification if reduced-wait is too aggressive.
