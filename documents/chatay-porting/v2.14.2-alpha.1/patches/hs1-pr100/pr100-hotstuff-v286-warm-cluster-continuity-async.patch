diff --git a/platform/consensus/ordering/hotstuff/commitment.cpp b/platform/consensus/ordering/hotstuff/commitment.cpp
--- a/platform/consensus/ordering/hotstuff/commitment.cpp
+++ b/platform/consensus/ordering/hotstuff/commitment.cpp
@@ -26,6 +26,7 @@
 
 #include <glog/logging.h>
 #include <unistd.h>
+#include <thread>
 
 #include "common/utils/utils.h"
 
@@ -565,7 +566,23 @@ int Commitment::ProcessMessageOnReplica(
                << " view=" << request->qc().node().info().view()
                << " proxy=" << request->qc().node().proxy_id()
                << " data_size=" << request->qc().node().data().size();
+    if (PrimaryId(current_view_) == id_) {
+      const int64_t next_view = current_view_;
+      std::thread([this, next_view]() {
+        const int64_t view_slot = next_view % 128;
+        {
+          std::unique_lock<std::mutex> lk(mutex_[view_slot]);
+          received_senders_[view_slot].clear();
+          received_requests_[view_slot].clear();
+        }
+        LOG(ERROR) << "CHATAY_HS1_TRACE warm_cluster_continue_async"
+                   << " self=" << id_
+                   << " view=" << next_view
+                   << " primary=" << PrimaryId(next_view);
+        ProcessNewView(next_view);
+      }).detach();
+    }
     message_manager_->Commit(std::move(request));
   } else {
     // For prepare, send back the new node in qc. Then every message in the
     // future, the new node will be included in QC.
